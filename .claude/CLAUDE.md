# graphify

- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
  When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# simulate-traffic

- **simulate-traffic** (`.claude/skills/simulate-traffic/SKILL.md`) - generates synthetic user sessions on polymorph.fyi for eval traffic. Trigger: `/simulate-traffic`
  When the user types `/simulate-traffic`, invoke the Skill tool with `skill: "simulate-traffic"` before doing anything else.
