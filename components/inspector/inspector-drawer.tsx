'use client'

import { InspectorPanel } from './inspector-panel'

/**
 * InspectorDrawer is a placeholder during Stage 1 of the canvas migration.
 * The old artifact-based drawer has been removed. A new canvas-aware
 * inspector drawer will be implemented in a later stage.
 */
export function InspectorDrawer() {
  // During Stage 1, the inspector drawer has no content to show.
  // It will be re-wired with canvas context in a later task.
  return null
}
